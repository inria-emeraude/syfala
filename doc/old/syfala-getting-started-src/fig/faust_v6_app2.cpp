[...]
// main program infinite loop infinite loop
  void run()
  {
    while (true) {
      //check if reset btn is pressed 
    while (true) {
         controlFPGA();
         UIhandler();
         fControlUI->update();
    }
  }
[...]
